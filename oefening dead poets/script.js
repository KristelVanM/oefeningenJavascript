/*var x = 10;
var y = "a"

alert (y === "b" || x >= 10);

const amount = 20
document.write (`<h1> The top ${ amount } movies </h1>`);


alert ("stand on table");

var myMaster = "O Master";
var stringRobin = prompt ("write 'O Master'");
var robin = stringRobin;

if(robin == myMaster){
alert ("MY MASTER");
}

else {
    alert ("try again");
}

while (stringRobin !== "O Master"){
   stringRobin = prompt ("try again, write 'O Master'");
}

*/

/*
var message = "please enter password";
var password = "";
var correctPassword = "azerty";

while (password !== correctPassword) {
   password = prompt (message);
}
alert ("open seame");*/


alert ("stand on table");

var myMaster = "O Master";
var robin = prompt ("write 'O Master'");

while (robin !== myMaster){
  robin = prompt ("try again, write 'O Master'");
}

alert ("MY MASTER");
    